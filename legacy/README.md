# Legacy (not Titanium play)

Historical αβ / CLI / perft helpers and the crate-root opening book.
Kept for tooling compatibility.

| Path | Contents |
|------|----------|
| `search/` | Historical αβ, CLI, perft TT (`crate::legacy_search`) |
| `opening/` | Crate-root opening book (`crate::opening`; not Titanium play book) |

Production play search lives in `src/titanium/search/`.
Production opening book lives in `src/titanium/opening/`.
Do not add new playing-engine code here.
