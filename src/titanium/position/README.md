# position

## Purpose

Board and game-state representation for Titanium play and tooling.

## Owns

`GameState`, packed states, board bridge, dataset state helpers.

## Uses

Core (board, zobrist), movegen/pathfinding as needed.

## Public API

`GameState`, pack/decode helpers — re-exported at `titanium::`.
